


/**
* getCurrentBlockNumber
*
* Description: get the current block time. 
* 	Given a blockchain, grab the first block time, add 
*	a block length to each subsiquent block, counting up to the current time.
* 	if blocks are missing the count still increments for that period. 
*/
int CBlock::getCurrentBlockNumber(){
	// Get first block.

	// Validate block data is not fraudulent.

	// Count up to current time. 

	// return block count.

	return 0;
}

